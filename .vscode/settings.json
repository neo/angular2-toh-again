{
  "files.exclude": {
    "*.*": true,
    "app/*.html": { "when": "$(basename).ts"},
    "app/*.css": { "when": "$(basename).ts"},
    "**/*.js": { "when": "$(basename).ts"},
    "**/*.js.map": true
  },
  "typescript.tsdk": "./node_modules/typescript/lib"
}
